# mcp-wallfacer

`mcp-wallfacer` is a dynamic fuzz-testing harness for MCP servers. The package installs a `wallfacer` CLI for local and CI-oriented validation.

This repository is bootstrapped for the v0.1.0 implementation plan.

